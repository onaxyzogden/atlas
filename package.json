{
  "name": "ogden-atlas",
  "private": true,
  "scripts": {
    "dev": "pnpm install && pnpm db:migrate && turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "db:migrate": "pnpm --filter @ogden/api db:migrate",
    "db:studio": "pnpm --filter @ogden/api db:studio"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}

# 2026-05-31 -- Act tier-shell objective-driven tools rail + field-action ops dashboard (Phase C)

**Branch.** `feat/atlas-permaculture` (three explicit-path slice commits `e6030252` tools-rail Parts A-D -> `90820e9e` ActOpsDashboard -> `94291d51` map-first surface + ActLayout swap; interleaved with foreign commits by the out-of-band rebase, HEAD advanced to `faf49f3a` with all three reachable; not pushed).

Two operator requests on the v3 Act surfaces, captured on the live bottom rail. **(1)** Replace the always-on three-log `ActTierToolsRail` (tier-shell) with a CATEGORIZED tools panel filtered by the currently-selected objective; field logs (harvest / water / livestock) become objective-conditional. **(2)** Port the prototype's ops-card dashboard into `field-action`, replacing the task-list `ViewBDashboard`. Three binding operator decisions (AskUserQuestion): author an EXPLICIT per-objective->tool map (net-new product data, not domain-reuse / not show-all); wire each tool to a REAL map placement/draw action now (not a stub); dashboard scope = field-action only (tier-shell keeps its task-list dashboard + toggle; `ViewBDashboard` preserved).

**Part A-B-C-D (tools rail, `e6030252`).** Net-new shared product data `packages/shared/src/relationships/objectiveActTools.ts` mirrors `objectiveObserveDomains.ts`: `OBJECTIVE_ACT_TOOLS_OVERRIDE` (per-objective) over `STRATUM_ACT_TOOLS_DEFAULT` (per-stratum backstop), resolved by `getObjectiveActTools(objective)` (override wins, else stratum default, else `[]`), returning catalogue-id STRINGS only (no icons / no `MapToolId`, so it stays app-dep-free) and exported from the ROOT `@ogden/shared` barrel (`src/index.ts`, staged via a hand-crafted single-hunk `git apply --cached --recount` because the working-tree file also carried a foreign "Protocol Layer" export block from a parallel session). Mapping: s1-vision / s1-stewardship -> `[]` (non-spatial -> empty state); s2-land-baseline -> contour, drainage, soil, vegetation, erosion; s3-systems-baseline -> roads, power, water-lines, gates, fencing, buildings; s4-zones-sectors -> roads, gates, fencing, buildings; s5-water-strategy -> water-lines, tanks, wells, water (log); s6-yield-flows -> crops, orchards, paddocks, beds, compost, harvest (log), livestock (log); s7-phasing -> buildings, barns, tanks. App-layer catalogue `apps/web/src/v3/act/tier-shell/actToolCatalog.ts` joins each id to `{label, icon, category, arm}` with a discriminated union `ActToolArm = {kind:'map';mapToolId:MapToolId} | {kind:'log';quickLogId:string}` -- every `mapToolId` literal is union-checked by `tsc` (the primary safety net for ~21 placement tools); the 3 logs reference existing `QUICK_LOGS` by id (no duplication); analysis-only `slope`/`aspect`/`dem` OMITTED (no draw tool, no store -- an un-armable button is worse than absence). New `ActTierCategorizedToolsRail.tsx` (fork of the preserved `ActTierToolsRail.tsx`) renders only the non-empty categories for the selected objective (collapsible, default expanded), armed-tile highlight from `useMapToolStore`, two empty states (no objective; non-spatial objective). `ActTierShell.tsx` mounts `ObserveDrawHost` + `PlanDrawHost` beside the existing `ActDrawHost` (each hard-guards on its id prefix, returns `null` otherwise; one tool armed at a time), keeps `PlanDataLayers editable={false}`, swaps the bottom rail, and generalizes `handleActivateTool` (map -> `setActiveTool`; log -> `setActiveModule`+`setActiveTool` via the resolved `QUICK_LOGS` entry).

**Part E (field-action ops dashboard, `90820e9e` + `94291d51`).** New `apps/web/src/v3/act/field-action/ActOpsDashboard.tsx` (+ module CSS) -- a permanent equivalent of the prototype-only `ActProtoDashboard`, stacking the four production-wired ops cards (WeatherStrip, TodaysPriorities, AlertsPanel, UpcomingEvents) in a flex column. Recovered the rebased-out Phase B `ActMapFirstLayout.tsx` (+ CSS) and pointed its no-objective branch at `<ActOpsDashboard projectId={id} />` (was `ViewBDashboard`); the objective branch still renders `ViewAObjectiveExecution`. `ActLayout.tsx` field-action mode now renders `ActMapFirstLayout` (was `ActFieldActionLayout` via `StageShell`); `ActFieldActionLayout` and `ViewBDashboard` both preserved on disk ([[feedback-no-deletion]]). Operator decisions this session: wiring swap -> "Commit it as mine"; dead API / screenshot -> "Restart API & verify"; push -> "Don't push".

**The ADR-7 tension.** Mounting Observe/Plan DrawHosts on the Act canvas crosses the stage boundary ("Act executes + collects; Plan decides"). Mitigated, not ignored: all three hosts write to the SHARED stores (`builtEnvironmentStoreV2`, crop/livestock/design-element) -- one source of truth, no Act-only shadow geometry; and `PlanDataLayers editable={false}` gates EDITING existing Plan features (its `if (!editable) return` guards live in the selection/edit effects), NOT new-feature drawing -- so Act can ADD placements but cannot EDIT Plan decisions (add-only). Closes the "per-objective tool relevance in the bottom rail" follow-up deferred by [[decisions/2026-05-30-atlas-act-tier-shell-promotion]].

**Errors fixed.** Two TS2339 closure-narrowing errors (`Property 'quickLogId' does not exist on type ActToolArm`) at `ActTierCategorizedToolsRail.tsx:37` + `ActTierShell.tsx:233` -- TS drops discriminant narrowing of a parameter property inside a nested `.find` callback; fixed by hoisting `const arm = tool.arm` before the closure in both files. A task-notification falsely reported tsc exit 0 while the output file showed real errors (read the file directly).

**Verified.** Web `tsc --noEmit` clean for all changed files (the only remaining web tsc errors are foreign `ORCHARD_SECONDARY_*` from a parallel session's uncommitted catalogues test); `@ogden/shared` tsc clean. Live preview (functional, DOM via `preview_eval` / `preview_inspect`): s2-land-baseline -> only the Terrain & Survey category (5 tools); s6-yield-flows -> Production Systems (5) + Field logs (harvest, livestock) with the water log correctly absent (it belongs to s5); no-objective -> empty state; arming "Paddocks" flipped its tile `data-active=true` AND mounted a real `PlanDrawHost` PADDOCK draw dock on the Act canvas (cross-stage arming proven end to end); field-action no-objective panel renders the four ops cards via `ActMapFirstLayout` (`reactComponent:"ActMapFirstLayout"`, `isTaskList:false`), not the task list. `preview_screenshot` UNRESPONSIVE (disclosed, not assumed): hung at 30s on the live MapLibre WebGL canvas even after the API:3001 was restarted to HTTP 200 ([[project-screenshot-hang]]); verified via DOM / component inspection instead. All three commits reachable from `faf49f3a` after the out-of-band branch advances.

Explicit-path commits, foreign WIP untouched ([[feedback-no-deletion]]); not pushed -- `git fetch` + divergence check before any push ([[project-branch-rebase]]); CSRA model untouched ([[fiqh-csra-erased-2026-05-04]]); ASCII-only copy. Entity [[entities/act-tier-shell]]; ADR [[decisions/2026-05-31-atlas-act-objective-tool-rail]]; continues [[decisions/2026-05-30-atlas-act-tier-shell-promotion]] and the same-day [[decisions/2026-05-31-atlas-act-objective-marker-geometry]].

# 2026-05-31 -- Plan Nav v1.1 deferred seams closed (secondary removal + Deferred status, greyed gate history, real Observe-stage observe-gap)

**Branch.** `feat/atlas-permaculture` (explicit-path slice commits; not pushed -- out-of-band rebase [[project-branch-rebase]], awaiting steward "go").

**Objective.** Build the three seams the v1.1 merge ([[decisions/2026-05-30-atlas-plan-nav-v1.1-merge]]) flagged out of scope: (C) secondary **removal** with a new `deferred` objective status (spec 8.3), (D) full greyed **previous-gate history**, (E) the real **Observe-stage render** of the observe-gap. Steward decisions: Deferred = alternative to removal (keeps the secondary, shelves objectives, dependents stay locked, removal stays blocked); build all three; Observe-gap = re-derived persistent surface (not a transient echo).

## Part C -- secondary removal + Deferred status (8.3) -- `0621d9b8`
- **Schema + status engine.** `'deferred'` added to `PlanStratumObjectiveStatus`. `computeObjectiveStatus`/`computeAllObjectiveStatuses` thread optional `deferredIds?: ReadonlySet<string>`: id in set -> short-circuit `deferred` (before prereq/progress); a `deferred` prereq counts as **not complete** so dependents stay `locked`. Backward-compatible (defaults empty).
- **Persisted Deferred state.** `planStratumStore` gained `deferredByProject: Record<string,string[]>` + `deferObjective`/`undeferObjective` (idempotent) + `selectDeferredObjectives` + `toDeferredSet`, in `partialize`, with the store's OWN persist version bump + additive migration backfilling `{}`. (Isolated -- projectStore stays v7.)
- **Removal action + preview.** `projectStore.removeSecondaryType -> { ok:true } | { ok:false, blockingObjectiveIds }`. Reuses `computeObjectivesDelta(afterRemoval, current)` (swapped) for removed objectives; blockers = current status (incl. deferredIds) in {active, complete, deferred}. Clean remove filters `secondaryTypeIds`, appends one `ProjectTypeVersion { action:'secondary-removed' }`, prunes orphaned progress, writes via `updateProject` (no persist bump). `useSecondaryRemovePreview` mirrors the add-preview, read-only.
- **UI.** "Manage project types" lists current secondaries + Remove; blocked remove opens `SecondaryRemoveBlockedModal` (amber) naming blockers as deep-links + "Mark as Deferred instead" CTA. Deferred cards greyed + Deferred pill + Restore, excluded from nextUp/parallel/sorted memos (list-only; status engine never mutated).

## Part D -- greyed previous-gate history -- `35c958bd`
- Schema: optional `completionGateBase?: string` + `completionGateAmendments?: { secondaryTypeId, text }[]` (additive; resolved objectives are runtime-only).
- `resolveProjectObjectives` Pass 2: before the existing `concatText` overwrite, capture `completionGateBase` on the FIRST amendment only + push each `{ secondaryTypeId, text }`. Flat `completionGate` concat unchanged.
- `DecisionChecklist` renders a greyed "Previously" block (base gate + per-amendment "<Type> added: <text>") when amendments exist; kept the amber "Amended by" badge; unamended objectives unchanged.
- Tests: `resolveProjectObjectives.gateHistory.test.ts` 7/7 (real residential single-amendment + synthetic multi-secondary ordering).

## Part E -- real Observe-stage observe-gap render (re-derived, persistent) -- `9a540d33`
- **Shared predicate (no drift).** New `relationships/observeGap.ts`: `isObserveGapObjective`/`objectiveHasInjectedItems`/`collectObserveGapObjectives`. Gap = `outputKind === 'observation-record'` OR injected items AND non-empty `defaultOverlayBundle`. "Injected items" signal is caller-supplied (a delta for the Plan add-preview, the persistent `expandedBySecondaryId` marker for the Observe re-derivation). `useSecondaryAddPreview` refactored onto it (behaviour-preserving).
- **Observe hook.** `useObserveGapObjectives`: pure derivation over resolved objectives + captured `ObserveDataPoint`s (like `usePlanRevisionFlagSync`). OPEN gap = gap objective whose primary domain (`getPrimaryDomainForObjective`) has no active (non-superseded) data point; clears on next render once the domain gains data. Grouped by domain.
- **Render.** `ObserveStageGapBanner` (persistent teal/info) in the `ObserveLayout` overlay slot beside `TrueNorthAdvisoryBanner`, deep-linking each mapped domain card. No dismiss (a still-open gap is not hideable).
- Tests: `observeGap.test.ts` 9/9 (shared); `useObserveGapObjectives.test.ts` 6/6 (web) -- domain mapping, grouping, superseded-only-keeps-open, gap-clears-on-capture, via synthetic gap objectives (mocked `useProjectObjectives`) + the real `observeDataPointStore`.

## LATENT-FEATURE finding (Part E)
Current catalogues carry **zero** objectives with an `observation-record` outputKind and **zero** with a non-empty `defaultOverlayBundle` -> no real project resolves a persistent observe-gap yet (`regenerative_farm` + `residential` = empty). Part E (and the v1.1 Plan-side banner) is a forward-looking seam, exercised only against synthetic data until such catalogue data ships. Discovered via a scratch probe; logged here so the seam is not mistaken for dead code.

## Verification
- `@ogden/shared` tsc exit 0. Web tsc: all Part E files clean; the 6 remaining `apps/web` tsc errors are **pre-existing foreign parallel-session WIP** (`plan/spine/DesignDetailPanel`, the incomplete `DecisionChecklist` decisionGroups render -- `GroupedChecklist`/`ItemRow` undefined, matching foreign commit `d69e244c` -- and `portfolio/observe-compare/observeCompareModel`); none in this session's surface.
- Scoped vitest: `observeGap` 9/9, `useObserveGapObjectives` 6/6, `resolveProjectObjectives.gateHistory` 7/7, plus Part C suites from `0621d9b8`. Per-file isolation (the full `apps/web` run hangs on a foreign timer suite).
- **Remaining:** preview DOM-exercise of (C)(D)(E) on a live `regenerative_farm` project, deferred to steward presence -- Part E in particular cannot render on current catalogue data (latent-feature note above).

## Covenant + discipline
- Only `planStratumStore` persist version bumped (Part C, additive migration). `projectStore` stays v7; no `ogden-projects` manifest schemaVersion bump. Parts D + E add no persisted state.
- Part E barrel routing: exported via `relationships/index.ts` + the `@ogden/shared/relationships` subpath; `packages/shared/src/index.ts` deliberately NOT edited (carries uncommitted foreign Act-tools exports) -- documented deviation from the plan's literal "re-point src/index.ts barrel".
- Every commit explicit-path; gated with `git status --short` + `git diff --cached --stat`; foreign parallel-session dirty set left untouched per [[feedback-no-deletion]]. ASCII-only; co-authored.
- Not pushed; `git fetch` + divergence check required before any push ([[project-branch-rebase]]).
- CSRA model untouched ([[fiqh-csra-erased-2026-05-04]]); covenant framing intact.

ADR: [[decisions/2026-05-31-atlas-plan-nav-v1.1-deferred-seams]]. Closes the deferred seams from [[decisions/2026-05-30-atlas-plan-nav-v1.1-merge]].

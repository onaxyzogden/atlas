# 2026-06-03 — Act-stage objective coverage audit + homestead tool overrides (R1/R3)

**Branch.** `feat/atlas-permaculture` (explicit-path commit `61a56ae6`, 4 files; **not pushed**, verified ancestor of HEAD after external rebase). Answered the operator's standing question — *does every single objective have an Act-stage tool that lets a user complete and record completion of its task set?* — with a deterministic audit instead of eyeballing ~200 objectives, then closed the highest-priority gap.

**Audit (read-only).** NEW `scripts/audit-act-objective-coverage.ts` (run via `tsx`) drives the SAME `@ogden/shared` resolvers the live UI uses (`resolveProjectObjectives`, `getObjectiveActTools`, `getObjectiveEvidence`, `getPrimaryDomainForObjective`) across all **14** types (13 primary + `residential` as a secondary layer, diffed against homestead-alone) and emits a per-objective coverage matrix to `scripts/audit-out/act-objective-coverage.md` (one row per objective: type, stratum, id, #tools, hasOverride, primaryDomain|null, #requiredEvidence + per-type/per-gap counts). Human-readable classification + remediation in `scripts/audit-out/act-coverage-findings.md`.

**Direct answer.** Every objective is completable AND recordable **today** via the universal `ActTierExecutionPanel` (Checklist→`planStratumStore.toggleItem`; Evidence→`actEvidenceStore` via `getObjectiveEvidence`, never empty; "Record observation"→emits an `ObserveDataPoint`, gated checklist+required-evidence+non-null domain). The three gap classes are about *precision*, not availability:
- **Gap A** (271 objectives at audit time) — objective falls through to the coarse `STRATUM_ACT_TOOLS_DEFAULT` instead of a precise per-type override. The exact misalignment that forced the silvopasture overrides on 2026-06-01; `OBJECTIVE_ACT_TOOLS_OVERRIDE` only keyed universal `s*` + `silv-*`, leaving all 12 other type catalogues coarse.
- **Gap B = 0** — the Record button is NEVER structurally blocked by a null primary Observe domain. No remediation needed.
- **Gap C** (41 at audit time) — `[]` bottom-rail tools: 11 intentional (decision/strategy objectives served by checklist + summary-note) + 30 accidental s1 vision/intent empties.

**R1 — homestead overrides (done).** Authored 15 explicit `hms-*` `OBJECTIVE_ACT_TOOLS_OVERRIDE` entries in `packages/shared/src/relationships/objectiveActTools.ts` (after the silvopasture block, before the closing `};`). **8 spatial objectives** grounded — each tool id verified against a real checklist item + a mountable `ACT_TOOL_CATALOG` tool: `hms-s2-resource-flows`→`[flow-connector]`; `hms-s2-productive-capacity`→`[vegetation,orchards,tanks,compost,fencing,soil]`; `hms-s2-landscape-vectors`→`[neighbour-pin,wind-sector,watercourse,hazard-zone]`; `hms-s3-water-quality`→`[water,catchment]`; `hms-s4-fertility-strategy`→`[compost,flow-connector]`; `hms-s5-food-zones-layout`→`[zone,beds,crops,orchards,buildings,compost,path]`; `hms-s5-energy-shelter-systems`→`[buildings,power,barns]`; `hms-s5-animal-husbandry`→`[barns,paddocks,fencing,compost,water-lines,tanks]`. **7 decision/financial objectives** set to an intentional, gap-noted `[]` (house-style `// gap:` comments) — `hms-s1-household-needs`, `hms-s4-food-production-strategy`, `hms-s4-energy-shelter-resilience`, `hms-s6-self-sufficiency-feedback`, `hms-s7-provision-phasing`, `hms-s7-budget-input-reduction`, `hms-s7-adaptive-management` — which ALSO corrected 6 objectives that previously showed misaligned stratum-default tools (e.g. buildings/barns/tanks on a budget objective).

**R2 — DEFERRED (by design).** Form-arm tools for the 30 accidental-empty s1 vision objectives are NOT fabricated: the existing form tools are hardwired to the universal `s1-vision-c*` formIds/prompts (reusing them on per-type s1 objectives is semantically wrong), and authoring new per-type form prompts = inventing operator-reviewed catalogue content (forbidden, [[feedback-csa-in-catalogues]] content-discipline). The universal `s1-vision` (with form arms) already resolves into every project, so completion is not blocked.

**R3 — ratchet (done).** Extended `apps/web/src/v3/act/tier-shell/__tests__/actToolCoverage.test.ts` with a homestead assertion (imports `HOMESTEAD_PRIMARY_OBJECTIVES`, asserts every `hms-*` objective has an explicit override entry; an explicit `[]` satisfies it — only a brand-new un-wired `hms-*` trips it), alongside the universal + silvopasture assertions.

**Verified.** Audit re-run after R1: Gap A 271→256, Gap B 0, Gap C 41→47 (18 intentional / 29 default-driven). Bounded per-workspace vitest `--pool=forks` ([[feedback-vitest-bounded-runs]]): `actToolCoverage` **6/6** (incl. the new homestead assertion), `objectiveObserveDomains` green, `resolveProjectObjectives` **24/25** — the 1 failure (expected 54, got 59) proven **pre-existing and unrelated** by `git stash` of my 2 edited files → still fails → `git stash pop`; root cause is the agritourism AG-S4.8 membership extension (`15680301`) whose count assertion was never updated. Flagged via a spawned task; NOT fixed (out of scope). Workspace tests must run from inside `apps/web` / `packages/shared` (root vitest only globs root `src/**/*.{js,jsx}`); pnpm not on Bash PATH so used `npx vitest` directly.

**Process.** Explicit-path commit of 4 files, never `git add -A`; `61a56ae6` confirmed ancestor of HEAD after the external rebase ([[feedback-commit-immediately-on-rebased-branches]], [[project-branch-rebase]]); not pushed; CSRA untouched ([[fiqh-csra-erased-2026-05-04]]); ASCII-only.

**Next.** The remaining 11 primary types (256 Gap-A objectives) wired with the same grounded-candidate method (verify each tool id against a real checklist item + a mountable catalogue tool; decision/strategy → gap-noted `[]`), then ratcheted into `actToolCoverage.test.ts` per type. Suggested order: regenerative_farm, market_garden, orchard. R2 stays deferred pending operator-designed per-type form-catalog content. The `olos_*` proof/verification backend stays unwired (separate epic).

ADR [[decisions/2026-06-03-olos-act-objective-coverage-audit]]; entity [[entities/act-tier-shell]].

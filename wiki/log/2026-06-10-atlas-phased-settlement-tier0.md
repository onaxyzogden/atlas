# 2026-06-10 -- SettlementCapture (Phase 3e-iii) wired into Tier-0 Act

**Objective:** Build and wire an advisory Tier-0 `SettlementCapture` for the ecovillage objective `ev-s4-settlement-strategy` (EV-S4.4), following the same advisory build-then-wire recipe as SoilImprovementCapture ([[log/2026-06-10-atlas-soil-improvement-tier0]]), WaterSystemsCapture ([[log/2026-06-10-atlas-water-systems-tier0]]), and EnergyCapture ([[log/2026-06-10-atlas-energy-systems-tier0]]). Settlement is the third and final of the Phase 3e batch (water / energy / settlement); like energy it is `source: 'primary'` (ecovillage), so it does NOT resolve for the Homestead-primary vertical slice -- sign-off is test + verbatim-diff, not live.

## What landed

**SettlementCapture (`ev-s4-settlement-strategy`, ecovillage EV-S4.4, 6 modes)** -- a Tier-6 Phasing & Resourcing advisory capture answering "who moves in when, under what conditions, and what infrastructure must be ready before each household cohort arrives?". Modes c1..c6: `cohort` (3 phased cohort cards, members + entry criteria) / `threshold` (hard/soft habitability thresholds per phase, hard-gate warn block) / `sequence` (3 phase timeline with milestones + gate connectors) / `trial` (trial-residency terms + 6 criteria + 3 outcomes) / `capacity` (water/land/governance carrying-capacity limits + summary ceiling) / `gates` (3 phase go/no-go gates with pass/blocked/pending state + hard-gate warn block).

- **Ecovillage objective => out-of-slice.** Like energy and unlike soil/water, `ev-s4-settlement-strategy` is `source: 'primary'` (ecovillage), so it does NOT resolve for the Homestead-primary slice. Sign-off rests on the 14 render unit tests + the verbatim mockup diff + the `/v3/components` gallery panels.
- **New component, authored not cherry-picked.** `SettlementCapture.tsx` + `.module.css` + `.test.tsx` (14 tests). Pure/controlled: `decode(mode,value)` models each render, `onChange(encode(next))`, decode never throws/fabricates (text fields default to `""`). Advisory contract mirrors Soil/Water/Energy: **no `projectId`, no store adapter**; passes `siblingValues` for cross-mode summary only (currently `void`-ed).
- **Habitability hard-gate honored without inventing an enforcement gate.** The objective's `scopeNotes` declares habitability thresholds (potable water, weathertight shelter, sanitation, emergency comms) as **hard gates** -- moving households onto the land before they are met creates welfare and legal risk. `isSettlementValid` stays **always-`true`** (advisory, consistent with water/soil/energy: this is a strategy-reading surface, not a runtime gate enforcer), and the hard-gate framing is surfaced **prominently as a warn `InterpretationBlock`** in the c2 (habitability) and c6 (go/no-go) bodies rather than as a record-blocking validity rule. Rationale is documented in code comments at the `isSettlement` derivation (`ActTierZeroWorkbench`), the `settlementMode` decode (`DecisionWorkingPanel`), and the gallery block (`ComponentsDebugPage`).
- **Verbatim mockup content.** All data constants transcribed verbatim from `OLOS UI/olos_phased_settlement.html` panels p1-p6 (mapping confirmed exact 1:1, p1->c1 .. p6->c6): cohort (P1 Founding 2 households / 4 adults / 2 dependants / Founding members + 4 named members; P2 Second +2 households gated on habitability + 6-month review; P3 Open membership, criteria TBD); threshold (Phase 1: 4 hard + 2 soft incl. potable water >= Phase 1 demand; Phase 2: 4 hard + 1 soft); sequence (3 phases Months 1-8 / 9-18 / 19-36 est., milestones legal/infra/social/land, gate connectors); trial (12 months, provisional lease, reduced contribution 50%, advisory voice no vote; 6 criteria 4-on/2-off; 3 outcomes Pass/Extend/Decline); capacity (Water 3,800 L/day ok; Land 5.1 ha limiting at > 6 households; Governance recommended max 8 households; summary max 6 households / ~14 people / Land limiting); gates (Phase 1 Open 4-pass; Phase 2 Blocked 4-unmet; Phase 3 Not-yet-active 4-pending incl. supermajority vote, all Hard). No fabricated data ([[project-slice-rescope]]). ASCII-only transcription (>= <= x -> m2 m3 ~, en-dash -> "-", apostrophes via double-quoted JS strings).
- **6-site workbench wiring:** `ActTierShell` `TIER_ZERO_OBJECTIVE_IDS`; `ActTierZeroWorkbench` `isSettlement` derivation + return field (flows through `buildDecisionTarget` -> `DecisionPanelTarget`); `DecisionWorkingPanel` import + interface flag + mode decode + validity arm + summary arm + body arm (passes `siblingValues`, **no `projectId`**); `workbenchAffordances` MAP entry (advisory: no strips, `showGroups:true`, returns `st-${m}`); `DecisionList` MODE_LABELS (6 entries); `ComponentsDebugPage` c1..c6 gallery.

## MODE_LABELS namespace (`st-`)

Following the `en-` (energy) / `wt-` (water) / `si-` (soil) / `li-` (livestock) / `hb-` (husbandry) precedent, the component's generic mode keys (`cohort` / `threshold` / `sequence` / `trial` / `capacity` / `gates`) are namespaced **`st-`** at the affordance layer (`workbenchAffordances.modeFor` returns `st-${settlementModeFor(itemId)}`), and `DecisionList` carries six matching labels (`st-cohort` "Cohort definition" / `st-threshold` "Habitability gates" / `st-sequence` "Arrival sequence" / `st-trial` "Trial residency" / `st-capacity` "Carrying capacity" / `st-gates` "Go/no-go gates" -- matching the catalogue `mode` badges verbatim). `DecisionWorkingPanel` routes off its own `settlementModeFor` independently.

## Verification

- web `tsc --noEmit` EXIT 0 (8 GB heap, from worktree `apps/web`); `@ogden/shared` `tsc --noEmit` EXIT 0.
- The dynamic CSS-module template-literal key access (`` css[`itone_${z.interpTone}`] ``, `` css[`ms_${badge}`] ``, `` css[`spNum_${p.num}`] ``) type-checks cleanly -- matches widespread existing repo precedent.
- Bounded vitest (`--pool=forks --no-file-parallelism --testTimeout=20000`, [[feedback-vitest-bounded-runs]]): SettlementCapture + DecisionWorkingPanel + ActTierZeroWorkbench suites **109 tests / 3 files green**, including the 14 new `SettlementCapture` tests (mode mapper c1..c6 + null/foreign-prefix, defensive decode -> `{kind, notes:''}` with no throw on garbage, lossless encode roundtrip, always-valid for all modes, non-empty summaries, and 6 render tests asserting verbatim mockup strings incl. both hard-gate warn blocks).
- **Live-preview limitation (honest):** ecovillage objectives are OUT of the active Homestead+Silvopasture slice, so `ev-s4-settlement-strategy` does not resolve in the running slice project; additionally the running `web` preview server (port 5200) is rooted on the **main atlas repo**, not this worktree. Sign-off rests on the 14 render unit tests (real React DOM via happy-dom) + the verbatim mockup diff + the new gallery panels, consistent with the water/soil/energy precedent.

## Amanah

**CLEAN.** A phased-settlement strategy is a community-foundation surface (Maqsid: Family / Ummah). The mockup carries member cost-sharing references transcribed verbatim ("Community fund solvent and contributions current from Phase 1 households"; "Community fund solvent - contributions current, capital reserve at agreed minimum"; trial "Reduced contribution (50%)"). **Reviewed and found clean:** these are ordinary co-owner cost-sharing among community members -- NOT riba, NOT gharar, NOT `bayʿ mā laysa ʿindak`, and NOT advance-purchase / CSRA / salam framing ([[fiqh-csra-erased-2026-05-04]]). No capital-channel or sale-channel construct requiring the "capital partners & allies" label is present. Encoded verbatim per the standing directive (never reword/omit); no `scopeNotes` flag required since no Amanah tension exists. (Finance proper -- `ev-s4-financial-model` + `ev-s7-financial-plan` -- is the next phase and will be Amanah-screened at kickoff.)

## State

Worktree `claude/husbandry-capture`. Slice **wired + verified but NOT yet committed/pushed** (commit/push only on explicit instruction, [[project-structured-capture-on-main]]). 3 new files (`SettlementCapture.tsx` / `.module.css` / `__tests__/SettlementCapture.test.tsx`) + 5 modified wiring files (`ActTierShell.tsx`, `ActTierZeroWorkbench.tsx`, `DecisionWorkingPanel.tsx`, `workbenchAffordances.ts`, `DecisionList.tsx`) + `ComponentsDebugPage.tsx` + this log. This completes the Phase 3e batch (water + energy + settlement); the prior energy/water captures remain uncommitted in the same worktree, awaiting an explicit commit/push instruction.

## Next session

Phase 3f finance: `ev-s4-financial-model` + `ev-s7-financial-plan`, **Amanah-screened at kickoff** -- member cost-sharing only (buy-in, levy, hardship deferral, reserve); no riba, no gharar, no `bayʿ mā laysa ʿindak` / CSRA / salam / advance-purchase; permitted MTC capital channels are charitable donation, restricted donation, qard ḥasan, in-kind contribution, sponsorship; public label "capital partners & allies"; encode any sale-channel ref verbatim + `scopeNotes` flag ([[fiqh-csra-erased-2026-05-04]]). Entity [[entities/act-tier-shell]].

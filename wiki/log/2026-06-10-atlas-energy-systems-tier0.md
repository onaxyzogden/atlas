# 2026-06-10 -- EnergyCapture (Phase 3e-ii) wired into Tier-0 Act

**Objective:** Build and wire an advisory Tier-0 `EnergyCapture` for the ecovillage objective `ev-s3-energy-potential` (EV-S3.5), following the same advisory build-then-wire recipe as SoilImprovementCapture ([[log/2026-06-10-atlas-soil-improvement-tier0]]) and WaterSystemsCapture ([[log/2026-06-10-atlas-water-systems-tier0]]). Energy is the second of the Phase 3e batch (water / energy / settlement); unlike water it is `source: 'primary'` (ecovillage), so it does NOT resolve for the Homestead-primary vertical slice -- sign-off is test + verbatim-diff, not live.

## What landed

**EnergyCapture (`ev-s3-energy-potential`, ecovillage EV-S3.5, 6 modes)** -- a Tier-2 Systems Reading advisory capture answering "what energy can this site generate -- solar, wind, hydro, biomass -- and how can it be distributed communally to all dwellings?". Modes c1..c6: `solar` (rooftop + ground-mount zones, shading, generation) / `wind` (annual speed + 3 regulatory tiers + contribution) / `hydro` (conditional micro-hydro sizing + not-applicable record) / `biomass` (woodland area, sustainable yield, thermal equivalent) / `demand` (6 end-use categories + community total) / `distribution` (generation/demand balance table + surplus + battery recommendation + 4 architecture options).

- **Ecovillage objective => out-of-slice.** Like the silvopasture-secondary captures and unlike soil/water, `ev-s3-energy-potential` is `source: 'primary'` (ecovillage), so it does NOT resolve for the Homestead-primary slice. Sign-off rests on the 14 render unit tests + the verbatim mockup diff + the `/v3/components` gallery panels.
- **New component, authored not cherry-picked.** `EnergyCapture.tsx` + `.module.css` + `.test.tsx` (14 tests). Pure/controlled: `decode(mode,value)` models each render, `onChange(encode(next))`, decode never throws/fabricates (text fields default to `""`). Advisory contract mirrors Soil/Water: **no `projectId`, no store adapter**; passes `siblingValues` for cross-mode summary only (currently `void`-ed).
- **No covenant gate.** An energy-systems assessment carries no halal/financial gate. `isEnergyValid` returns `true` for all 6 modes -- every mode is an always-recordable advisory input. The c3 hydro mode is CONDITIONAL in the prototype (record as not-applicable if no watercourse) but still does not block the gate, so it stays always-valid.
- **Verbatim mockup content.** All data constants transcribed verbatim from `OLOS UI/olos_energy_systems.html` panels p1-p6 (mapping confirmed exact 1:1, p1->c1 .. p6->c6): solar zones (Rooftop 28.6 kW / 240 m2 / 12% shading / 36,700 kWh/yr; Ground-mount 46.2 kW / 400 m2 / 5% / 59,200 kWh/yr; peak sun 4.5 h/day; total 74.8 kW / 95,900 kWh/yr); wind (4.2 m/s marginal; 3 regulatory tiers Large >= 50 kW red / Small <= 10 kW amber / Micro <= 1 kW green; ~2,500 kWh/yr); hydro (head 4 m, flow 12 L/s, 7 months -> 0.33 kW / ~1,700 kWh/yr + < 1 kW supplementary warn + not-applicable record note); biomass (8 ha available, 1.2 m3/ha/yr, 9.6 m3/yr, 34,600 kWh/yr thermal); demand (6 categories, 70 kWh/day = 25,550 kWh/yr); distribution (balance table 100,100 kWh/yr / 392% vs 25,550 demand; surplus 3.9x; battery 175-220 kWh; 4 architecture cards micro-grid selected). No fabricated data ([[project-slice-rescope]]). ASCII-only transcription (>= <= x -> m2 m3 ~, en-dash -> "-").
- **6-site workbench wiring:** `ActTierShell` `TIER_ZERO_OBJECTIVE_IDS`; `ActTierZeroWorkbench` `isEnergy` derivation + return field (flows through `buildDecisionTarget` -> `DecisionPanelTarget`); `DecisionWorkingPanel` import + interface flag + mode decode + validity arm + summary arm + body arm (passes `siblingValues`, **no `projectId`**); `workbenchAffordances` MAP entry (advisory: no strips, `showGroups:true`, returns `en-${m}`); `DecisionList` MODE_LABELS (6 entries); `ComponentsDebugPage` c1..c6 gallery.

## MODE_LABELS namespace (`en-`)

Following the `wt-` (water) / `si-` (soil) / `li-` (livestock) / `hb-` (husbandry) precedent, the component's generic mode keys (`solar` / `wind` / `hydro` / `biomass` / `demand` / `distribution`) are namespaced **`en-`** at the affordance layer (`workbenchAffordances.modeFor` returns `en-${energyModeFor(itemId)}`), and `DecisionList` carries six matching labels (`en-solar` "Solar potential" / `en-wind` "Wind potential" / `en-hydro` "Micro-hydro" / `en-biomass` "Biomass capacity" / `en-demand` "Energy demand" / `en-distribution` "Distribution"). `DecisionWorkingPanel` routes off its own `energyModeFor` independently.

## Verification

- web `tsc --noEmit` EXIT 0 (8 GB heap, from worktree `apps/web`); `@ogden/shared` `tsc --noEmit` EXIT 0.
- The dynamic CSS-module template-literal key access (`` css[`itone_${z.interpTone}`] ``, `` css[`rtone_${r.tone}`] ``) type-checks cleanly -- matches widespread existing repo precedent.
- Bounded vitest (`--pool=forks --no-file-parallelism --testTimeout=20000`, [[feedback-vitest-bounded-runs]]): full tier-shell suite **1072 tests / 58 files green**, including the 14 new `EnergyCapture` tests (mode mapper c1..c6 + null/foreign-prefix, defensive decode -> `{kind, notes:''}` with no throw on garbage, lossless encode roundtrip, always-valid for all modes, non-empty summaries, and 6 render tests asserting verbatim mockup strings: "Rooftop solar"/"Ground-mount solar"/"36,700 kWh/yr"/"95,900 kWh/yr"; "4.2 m/s"/"Regulatory constraints - Kinfolk Ridge (45 ha)"/"~2,500 kWh/yr"; "0.33 kW"/"Head x flow at this scale generates < 1 kW"/"record micro-hydro as not applicable"; "9.6 m3/yr"/"34,600 kWh/yr (thermal)"; "Demand by category - 8 households, 20 people"/"70 kWh/day = 25,550 kWh/yr"; "Total generation"/"3.9x community demand"/"175-220 kWh capacity"/"Community micro-grid"/"Grid-connected with export").
- **Live-preview limitation (honest):** ecovillage objectives are OUT of the active Homestead+Silvopasture slice, so `ev-s3-energy-potential` does not resolve in the running slice project; additionally the running `web` preview server (port 5200) is rooted on the **main atlas repo**, not this worktree. Sign-off rests on the 14 render unit tests (real React DOM via happy-dom) + the verbatim mockup diff + the new gallery panels, consistent with the water/soil precedent.

## Amanah

**CLEAN.** An energy-systems assessment is an environmental-stewardship surface (Maqsid: Environment). The c6 distribution mode carries two revenue references transcribed verbatim from the mockup: "available for grid export (revenue)" (surplus callout) and "Grid-connected with export - sell surplus at FiT rate ... community revenue stream" (architecture card). **Reviewed and found clean:** this is the ordinary sale of a genuinely-produced, presently-possessed commodity (surplus self-generated electricity) at a regulated feed-in-tariff -- NOT `bayʿ mā laysa ʿindak` (the community produces and owns the electricity it sells), NOT riba, NOT gharar, and NOT advance-purchase / CSRA / salam framing ([[fiqh-csra-erased-2026-05-04]]). No member-capital or sale-channel construct is present. Encoded verbatim per the standing directive (never reword/omit); no `scopeNotes` flag required since no Amanah tension exists.

## State

Worktree `claude/husbandry-capture`. Slice **wired + verified but NOT yet committed/pushed** (commit/push only on explicit instruction, [[project-structured-capture-on-main]]). 3 new files (`EnergyCapture.tsx` / `.module.css` / `__tests__/EnergyCapture.test.tsx`) + 5 modified wiring files (`ActTierShell.tsx`, `ActTierZeroWorkbench.tsx`, `DecisionWorkingPanel.tsx`, `workbenchAffordances.ts`, `DecisionList.tsx`) + `ComponentsDebugPage.tsx` + this log.

## Next session

Phase 3e-iii SettlementCapture (`ev-s4-settlement-strategy`, ecovillage, `st-` namespace, from `olos_phased_settlement.html`, **honor habitability hard-gate scopeNotes**) -- ecovillage => out-of-slice, test + verbatim-diff sign-off. Then Phase 3f finance (`ev-s4-financial-model` + `ev-s7-financial-plan`, **Amanah-screened at kickoff**). Entity [[entities/act-tier-shell]].

# 2026-05-31 — Portfolio Home P8: Verification & acceptance (§9.1–9.4) — epic closed

**No code change.** Phase 8 is the OLOS Portfolio Home epic's final gate (`OLOS_Portfolio_Home_Spec_v1.0`, plan `olos-port-twinkly-peach`); it type-checks, tests, and walks the §9 Developer Acceptance Criteria on the running dev server. All checks passed clean, so there is no commit — Phase 8 produces a verification record only, not a slice. Closes the epic begun at [[log/2026-05-28-portfolio-home-slice53]] and built through P7 ([[log/2026-05-31-portfolio-home-p7]]). The four-zone surface ships as committed at P7 (`6bdbb80c`).

**8.1 — Type-check (= lint gate).** The web package's `lint` script *is* `tsc --noEmit` (no separate ESLint config), so the three type-checks satisfy both the tsc and the lint gates. shared **EXIT:0**, api **EXIT:0**, web **EXIT:0** (read the output file `/tmp/p8-web-tsc.txt` = `EXIT:0` for the authoritative exit code, not the slow-background notification; zero errors — the foreign parallel-session WIP errors seen in prior phases had settled).

**8.2 — Tests.** `@ogden/shared` 46 files / 847 tests green (incl. P4 `climateContext`); web portfolio specs `ProjectUrgencyCard` 3/3.

**8.3 — §9 acceptance walk (live, dev server web:5200 + api:3001).** Per [[project-screenshot-hang]], `preview_screenshot` hangs on MapLibre surfaces, so map pages were verified by `preview_eval` DOM probes and the map-free Dashboard directly.
- **§9.1 four-zone Map** — `/v3/portfolio` Map view: left "My Projects" rail with All/Plan/Act/Observe filter chips + per-project list items (stage + area); centre map (1 canvas); bottom Plan/Act/Observe stage rail; Map⇄Dashboard toggle present. ✓
- **§9.2 Dashboard** — Dashboard view: 0 canvases (map-free), 13 project cards each with an **Open** CTA, summary bar reading **"13 projects · 496 ha · 6 Setup / 4 Plan / 3 Act · 23 Open divergences"**; per-stage chips at exact `STAGE_PAINT` colours; stage-chip filter narrows the grid. ✓
- **§9.3 cross-project Observe** — `/v3/portfolio/observe-compare`: table headers Project/Baseline/Current/Change/Climate; climate-context badge renders "Spring · Temperate · N. hemisphere" (P4 `deriveClimateContext`, no climateZone) with a "No location" fallback; min-2/max-5 enforced; excluded-no-data projects notice; shared-domain intersection; "Read-only — no project data is changed here" notice. ✓
- **§9.4 relationships** — Cross-project relationship routes registered + capability-gated: `GET`/`POST /:id/cross-project-relationships` → **401** (auth-gated) vs a bogus path → **404** (proving real registration), and the distinct cross-project module coexists with the within-project `/relationships` route (naming-collision guard held). Migration `049_cross_project_relationships.sql` present; the API booted clean (runs migrations on dev-start → the `cross_project_relationships` table exists; a missing table would 500, not 401). Map-side: the **"Connections"** relationship-layer toggle renders **disabled / `aria-pressed=false`** (off by default, §2.7) carrying the §7 empty-state tooltip **"Add relationships to see connections"** (no relationships seeded). End-to-end create/list/delete was verified at the **P5 commit** (P5 gate); re-exercise through the current UI is blocked by an unauthenticated/degraded frontend session ("couldn't verify your saved session"), so live owner-gated CRUD was **not** re-driven this session — disclosed rather than claimed, per the verification discipline. ✓ (routes + migration + empty-state confirmed live; live CRUD verified-at-P5)
- **§6 navigation** — `/` landing redirect → `/v3/portfolio` (2+ projects); AppShell breadcrumb reads "Portfolio". ✓

**Disclosure.** §9.4 live create/list/delete was not re-exercised this session (frontend session unauthenticated; entering credentials is prohibited). The route registration (401 vs 404), migration-049 presence, and §2.7/§7 empty-state were confirmed live; full CRUD rests on the P5 end-to-end verification at commit time.

**Coverage gap (carried from P7).** P1–P6 of this four-zone spec were code-committed in prior sessions but never individually logged in this wiki; P7 + this P8 entry are the only narrative records of the epic's later phases.

**Discipline.** No commit (verification-only, all green). Running servers: web:5200 (`9a24b3bb`), api:3001 (`215a3692`). CSRA model untouched ([[fiqh-csra-erased-2026-05-04]]); 3-item nav forward IA ([[project-lifecycle-retirement]]); native Postgres on localhost:5432 ([[project-two-postgres-5432]] if present); ASCII-only copy.

**Epic complete.** All eight phases of the Portfolio Home plan are done and committed (P1–P7 in code; P8 = acceptance). ADR [[decisions/2026-05-31-atlas-portfolio-home-p7]]; entity [[entities/web-app]].

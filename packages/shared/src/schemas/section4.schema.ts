import { z } from 'zod';

// Section 4 — Site Assessment & Diagnostic Atlas
// Generated stub. Replace with the real Zod types as this section
// takes shape. Keep input/output types colocated.

export const SiteAssessmentPlaceholder = z.object({});
export type SiteAssessmentPlaceholder = z.infer<typeof SiteAssessmentPlaceholder>;

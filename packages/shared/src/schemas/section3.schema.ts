import { z } from 'zod';

// Section 3 — Site Data Layers & Environmental Inputs
// Generated stub. Replace with the real Zod types as this section
// takes shape. Keep input/output types colocated.

export const SiteDataLayersPlaceholder = z.object({});
export type SiteDataLayersPlaceholder = z.infer<typeof SiteDataLayersPlaceholder>;

/**
 * App.tsx — kept for backward compatibility.
 * The actual app is initialized in main.tsx with RouterProvider.
 * AppShell.tsx is the root layout component used by the router.
 */

export { default } from './AppShell.js';

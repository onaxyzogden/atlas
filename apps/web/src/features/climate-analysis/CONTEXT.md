# CONTEXT — Section 6: Solar, Wind & Climate Analysis

> Generated by `scaffold-section.ts`, populated by the Section 6 scaffold agent.
> This file satisfies the MILOS Context-First Protocol for `apps/web/src/features/climate-analysis/`.
>
> NOTE: This scaffolded folder is the canonical route/manifest surface for
> Section 6. A legacy climate UI folder already exists at
> `apps/web/src/features/climate/` (SolarClimateDashboard + SolarClimatePanel +
> ClimateScenarioOverlay) — it is consumed by `features/dashboard/DashboardRouter.tsx`
> and the map view. A later follow-up session will either redirect consumers
> here or move the legacy `climate/` folder into this scaffolded path. Do NOT
> duplicate the UI — treat this stub as a placeholder until that cut-over.

## Phase tags
P1, P2 (see `packages/shared/src/featureManifest.ts` for the authoritative item list)

## Purpose
Surfaces the solar, wind, frost, and microclimate dimensions of a site so the
land designer can place orchards, windbreaks, buildings, and outdoor-use
zones with seasonal sun-angle awareness, prevailing-wind shelter logic, frost
pocket avoidance, and climate-adaptation recommendations. Consumes the Tier-3
microclimate pipeline (frost pocket, cold-air drainage, sun trap, wind
shelter) and the per-station climate normals / Penman-Monteith PET feeds
assembled by the NOAA / NASA POWER / ECCC / OpenMeteo adapter stack.

## File inventory

### Scaffolded surface (this folder)
- `ClimateAnalysisPage.tsx` — route-level entry (stub)
- `index.ts` — default-export re-export (stub)

### Existing implementation (legacy path — `apps/web/src/features/climate/`)
- `SolarClimateDashboard.tsx` / `.module.css` — right-rail dashboard
  surfacing climate normals, PET, frost/wind flags; mounted by
  `features/dashboard/DashboardRouter.tsx`
- `SolarClimatePanel.tsx` / `.module.css` — inline panel variant consumed
  by the site-assessment surface
- `ClimateScenarioOverlay.tsx` — map overlay hook for GAEZ RCP scenario
  toggling (used by `features/map/MapView.tsx`)

### Server-side backing (API)
- `apps/api/src/services/terrain/MicroclimateProcessor.ts` — frost pocket,
  cold-air drainage, sun trap, wind shelter, outdoor comfort
- `apps/api/src/services/terrain/algorithms/microclimate.ts`,
  `frostPocket.ts`, `coldAirDrainage.ts` — pure algorithm modules
- `apps/api/src/services/pipeline/adapters/NoaaClimateAdapter.ts` — US
  climate normals (Tier 1)
- `apps/api/src/services/pipeline/adapters/NasaPowerAdapter.ts` +
  `nasaPowerFetch.ts` — global fallback, Penman-Monteith PET
- `apps/api/src/services/pipeline/adapters/EcccClimateAdapter.ts` — Canada
  climate normals
- OpenMeteo fallback wired inside the adapter dispatch (no dedicated file)
- Migration 008_erosion_cutfill.sql ships the microclimate output tables
  (frost_pockets, sun_traps, wind_shelter, cold_air_drainage)

### Shared engine
- `packages/shared/src/scoring/computeScores.ts` emits a
  `climate-resilience` label that already consumes frost, wind, and PET
  signals; parity-pinned by `apps/api/scripts/verify-scoring-parity.ts`

## Stores touched
- `useProjectStore` — active project id, boundary, layer summaries (read
  by the legacy `features/climate/` surface)
- `useAssessmentStore` (in `features/assessment/`) — cached
  `site_assessment` row that currently surfaces frost/wind flags; the
  canonical Climate dashboard will subscribe here for the microclimate
  summary until a dedicated store is introduced

## API routes consumed
- `GET /api/v1/climate-analysis` — scaffolded stub (returns empty list);
  no real handlers yet
- `GET /api/v1/projects/:id` — existing endpoint hydrates the
  microclimate outputs + climate normals piggy-backed on the site
  assessment blob
- `GET /api/v1/gaez/*` — GAEZ RCP scenario layers consumed by the
  climate scenario overlay
- `GET /api/v1/elevation/*` — terrain inputs upstream of microclimate
  (aspect / slope / cold-air sink analysis)

## Shared schemas
- `@ogden/shared` — `ProjectSummary`, `LayerSummary` union (climate
  normals + microclimate variants)
- `@ogden/shared/scoring` — `computeScores`, `climate-resilience` label
- `packages/shared/src/schemas/section6.schema.ts` — placeholder Zod
  stub; expand once the climate dashboard shape is lifted into
  @ogden/shared

## Cross-section dependencies
- §2 Basemap / §3 Data Layers — climate-normals layer availability
- §4 Site Assessment — consumes frost/wind/sun-trap flags produced by
  `MicroclimateProcessor`; also surfaces climate-resilience scored label
- §5 Hydrology — PET + precipitation feed pond/swale + flood logic
- §7 Soil & Ecology — microclimate zones influence restoration priority
- §9 Structures — passive-solar building siting reads sun-path + wind
- §12 Crops — seasonal comfort / outdoor-use seasonality informs
  planting windows
- §18 AI — climate adaptation recommendations will cite these inputs
  through the narrative pipeline

## Known gotchas
- Two folders claim ownership of the climate UI today: the legacy
  `apps/web/src/features/climate/` (shipped, consumed by DashboardRouter
  and MapView) and this scaffolded `apps/web/src/features/climate-analysis/`
  (stub, not yet mounted in routing). Do not duplicate components — the
  cut-over is a follow-up task. Use the legacy path for any real UI change.
- NASA POWER adapter is the international-routing fallback for climate
  normals outside US/Canada (see `NasaPowerIntlRouting.test.ts`); sun-path
  + solar-exposure heatmap UI items are `planned` (no shipped visualizer)
  even though solar radiation inputs are available from NASA POWER.
- Frost pocket detection is shipped end-to-end (migration 008 ->
  MicroclimateProcessor -> site assessment flag -> dashboard badge), so
  the `frost-pocket-heat-sink` feature is `done` for frost; heat-sink
  identification is bundled with it but only the frost half is wired
  today — revisit if the spec requires them split.
- Wind shelter is computed server-side but no wind-rose / prevailing-wind
  visualizer ships yet — status is `partial`, not `done`.
- Microclimate opportunity map is `partial`: the backend classifies sun
  trap / cold-air drainage / wind shelter zones, but the dedicated map
  overlay rendering those classes is still on the UI backlog.
- Passive-solar building siting, windbreak opportunity zones, ventilation
  corridors, and structure/tree shadow casting are P2 and require an
  obstacle model (trees + structures) that is not yet captured in the
  data layers — they depend on §9 Structures reaching `partial` first.

## Test surface
- `apps/api/src/tests/NoaaClimateAdapter.test.ts`,
  `NasaPowerAdapter.test.ts`, `EcccClimateAdapter.test.ts`,
  `NasaPowerIntlRouting.test.ts` — adapter contracts
- `apps/api/src/services/terrain/__tests__/` — microclimate,
  frost pocket, cold-air drainage unit tests
- `apps/api/scripts/verify-scoring-parity.ts` — pins the
  climate-resilience scored label; must remain green
- Highest-value test to add for this folder: a smoke test that
  `GET /api/v1/climate-analysis` returns 200 under `ATLAS_PHASE_MAX=P1`
  and 404 under lower caps (trust `featureGatePlugin`).

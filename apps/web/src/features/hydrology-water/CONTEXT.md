# CONTEXT — Section 5: Hydrology & Water Systems Planning

> Generated by `scaffold-section.ts`, populated by the Section 5 scaffold agent.
> This file satisfies the MILOS Context-First Protocol for `apps/web/src/features/hydrology-water/`.
>
> NOTE: This scaffolded folder is the canonical route/manifest surface for
> Section 5. The real implementation of the Hydrology UI currently lives at
> `apps/web/src/features/hydrology/HydrologyPanel.tsx` plus the
> `apps/web/src/features/dashboard/pages/HydrologyDashboard.tsx` view, backed
> by the terrain pipeline services listed below. A later follow-up session
> will either redirect consumers here or move the legacy `hydrology/` folder
> into this scaffolded path. Do NOT duplicate the UI — treat this stub as a
> placeholder until that cut-over.

## Phase tags
P1, P2 (see `packages/shared/src/featureManifest.ts` for the authoritative item list)

## Purpose
Visualizes and plans every water-relevant layer of a property: surface flow
paths, watershed and catchment boundaries, drainage network, flood
accumulation, pond/swale/berm/check-dam candidate zones, overflow routing,
roof catchment and rainwater storage sizing, gravity-fed irrigation and
livestock water access, wetland and riparian restoration, seasonal water
budget, and retention/drought/storm resilience scoring. This is the water
"design atlas" that sits between Section 4 (diagnostic) and the design
sections that place structures, crops, livestock, and access routes.

## File inventory

### Scaffolded surface (this folder)
- `HydrologyWaterPage.tsx` — route-level entry (stub)
- `index.ts` — default-export re-export (stub)

### Existing implementation (legacy paths)
- `apps/web/src/features/hydrology/HydrologyPanel.tsx` — legacy panel that
  surfaces the pipeline's hydrology outputs (watershed, drainage, flow
  accumulation) for the active project
- `apps/web/src/features/hydrology/HydrologyPanel.module.css` — layout
- `apps/web/src/features/dashboard/pages/HydrologyDashboard.tsx` — dashboard
  view over the same data (flow visualization, watershed, drainage)
- `apps/web/src/features/dashboard/pages/HydrologyDashboard.module.css`

### Server-side backing (API)
- `apps/api/src/services/terrain/WatershedRefinementProcessor.ts` — watershed
  delineation, catchment area, drainage lines, and pond / swale / berm /
  check-dam candidate generation (Tier-3 pipeline)
- `apps/api/src/services/terrain/TerrainAnalysisProcessor.ts` — TWI flow
  accumulation, flood-prone flagging, curvature inputs used by the
  watershed refiner
- `apps/api/src/services/terrain/algorithms/hydro.ts` — drainage line
  extraction algorithm
- `apps/api/src/adapters/NHDAdapter.ts` — NHD national drainage network
  (US)
- `apps/api/src/adapters/OhnAdapter.ts` — Ontario Hydro Network (CA
  equivalent), surfaced via Country INTL routing
- `apps/api/src/adapters/NwiFemaAdapter.ts` — NWI wetlands + FEMA
  floodplain overlays
- `apps/api/src/adapters/SsurgoAdapter.ts` — drainage class per soil map
  unit (feeds water retention / runoff modeling)
- `apps/api/src/db/migrations/007_twi_tri.sql` — persists TWI/TRI and
  related flow accumulation outputs
- `apps/api/src/db/migrations/008_erosion_cutfill.sql` — erosion +
  cut/fill detector (downstream of flood accumulation)

### Shared engine
- `packages/shared/src/scoring/*` — the shared scoring engine is the
  eventual home for `water-retention-drought-storm-scores`; at the time
  of this scaffold those items are `planned`, not computed.

## Stores touched
- `useProjectStore` — active project id, boundary, layer summaries
  (drainage / watershed / wetland layers surface here today)
- Legacy hydrology panel reads directly from the pipeline-hydrated
  project blob; no dedicated Zustand slice exists yet for this section.

## API routes consumed
- `GET /api/v1/hydrology-water` — scaffolded stub (returns empty list);
  the real read path today goes through `GET /api/v1/projects/:id`
  which hydrates watershed, drainage, and pond/swale candidate rows
  written by the Tier-3 pipeline.
- `GET /api/v1/elevation/*` — DEM underpinning flow / watershed / TWI
- `GET /api/v1/layers/*` — wetland, floodplain, NHD/OHN drainage
  layer access
- `GET /api/v1/pipeline/*` — pipeline job status for Tier-3
  watershed / TWI runs
- (Pond/swale suggestion, rainwater sizing, water budget, phasing,
  and retention/drought/storm scoring endpoints do NOT exist yet;
  those items are `planned` in the manifest.)

## Shared schemas
- `@ogden/shared` — `ProjectSummary`, `LayerSummary` union (drainage /
  watershed / wetland variants)
- `@ogden/shared/scoring` — reserved for future water-retention /
  drought / storm resilience scoring (not yet wired)
- `packages/shared/src/schemas/section5.schema.ts` — placeholder Zod
  types for this section's inputs/outputs (expand once legacy
  `hydrology/` panel shapes are lifted into @ogden/shared)

## Cross-section dependencies
- §2 Basemap — terrain / contour / hillshade layers used to render flow
  paths and watershed boundaries
- §3 Data Layers — NHD / OHN / NWI / FEMA / SSURGO drainage feed this
  section
- §4 Site Assessment — reads flood-prone flag, water resilience score,
  and pond/swale candidate zones for the diagnostic dossier
- §7 Soil & Ecology — drainage class and infiltration inputs for
  water retention scoring
- §11 Livestock — trough placement + livestock water access (Section 5
  P2 plans the water side; placement UX lives in §11)
- §12 Crops — gravity-fed irrigation layout feeds crop plan
- §15 Timeline / §16 Simulation — water system phasing and dependency
  mapping (planned) flows into timeline and scenario simulation

## Known gotchas
- Two folders claim ownership of Hydrology UI today: the legacy
  `apps/web/src/features/hydrology/` + the `HydrologyDashboard` dashboard
  page (shipped, consumed) and this scaffolded
  `apps/web/src/features/hydrology-water/` (stub, not yet mounted in
  routing). Do not duplicate components — the cut-over is a follow-up
  task. Use the legacy paths for any real UI change.
- Pond/swale/berm/check-dam items are `partial`: the candidate zones
  are computed by `WatershedRefinementProcessor` and persisted, but the
  UI "placement suggestion" flow (pick a candidate → propose a
  structure) is not shipped. Do not mark this `done` until the design
  surface exists.
- `water-retention-drought-storm-scores` is `planned`, not `partial` —
  the shared scoring engine currently ships the 10 Section-4 labels,
  and the three water-resilience scores in this section have not been
  added to `computeScores` yet. Adding them is a cross-cut change
  (engine + parity test + UI) and belongs to a dedicated session.
- Country-specific drainage networks: US = NHD, CA = OHN. Consumers of
  drainage data must go through the Country INTL routing layer, not
  hardcode NHD. See the `IntlCountryAdapter` pattern introduced in
  commit `0ea1351`.
- `GET /api/v1/hydrology-water` is gated behind `fastify.requirePhase('P1')`
  per the generator default — if `ATLAS_PHASE_MAX` is set below P1 the
  route 404s; trust `featureGatePlugin`, don't reimplement gating in
  the handler.

## Test surface
- `apps/api/src/services/terrain/__tests__/` — WatershedRefinementProcessor
  and TerrainAnalysisProcessor unit tests cover the existing flow,
  watershed, drainage, TWI, and candidate-zone outputs.
- `apps/api/scripts/verify-scoring-parity.ts` — must remain green; even
  though water scores are `planned`, any future addition must preserve
  parity between the shared engine and the server writer.
- Highest-value tests to add for this folder: a smoke test that
  `GET /api/v1/hydrology-water` returns 200 under `ATLAS_PHASE_MAX=P1`
  and 404 under `ATLAS_PHASE_MAX` below P1 (trust `featureGatePlugin`).

# CONTEXT — Section 28: Advanced Geospatial / Future-Ready Features

> Generated by `scaffold-section.ts`. Fill each section before adding code.
> This file satisfies the MILOS Context-First Protocol for `apps/web/src/features/future-geospatial/`.

## Phase tags
FUTURE (see `packages/shared/src/featureManifest.ts` for the authoritative item list)

## Purpose
One paragraph: what user-facing problem this section solves.

## File inventory
- `future-geospatialPage.tsx` — route-level entry
- `components/` — section-private components
- `hooks/` — section-private hooks
- `future-geospatialStore.ts` — Zustand slice (if section owns client state)

## Stores touched
- (List every `use*Store` consumed or mutated. If none, write "none".)

## API routes consumed
- `GET /api/v1/future-geospatial` — …
- (List every endpoint. If none, write "none — client-only".)

## Shared schemas
- `@ogden/shared` — (list imported types)
- `@ogden/shared/scoring` — (only if the section reads scores)

## Cross-section dependencies
- (Sections this one reads from. Keep to a minimum.)

## Known gotchas
- **Gated behind `ATLAS_FUTURE=1`**: every route under `/api/v1/future-geospatial`
  and the web feature surface will 404 unless the `ATLAS_FUTURE` env flag is set
  to `1`. All 9 features in this section are phase=`FUTURE`, meaning they are
  explicitly not day-one scope; they represent the true Atlas horizon
  (LiDAR / point clouds, 3D tiles, sensor telemetry, satellite change
  detection, AR/VR walkthrough, multi-property + portfolio planning).
  Do not wire these into default navigation or marketing copy without an
  explicit product-gate decision.
- The `FUTURE` phase is accepted by `scaffold-section.ts` and
  `featureManifest.ts` but is not part of the default `requirePhase` ladder —
  any handler using `fastify.requirePhase('FUTURE')` must also be guarded by
  the env check at app bootstrap.
- (Anything non-obvious a future planner must not trip on — legacy data
  shapes, race conditions, feature-flag interactions.)

## Test surface
- (Which `*.test.ts` files cover this section. If none yet, list the
  highest-value tests to add first.)

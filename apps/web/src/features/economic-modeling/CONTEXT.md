# CONTEXT — Section 22: Economic Planning & Business Modeling

> Generated by `scaffold-section.ts`. Fill each section before adding code.
> This file satisfies the MILOS Context-First Protocol for `apps/web/src/features/economic-modeling/`.

## Phase tags
P2, P3 (see `packages/shared/src/featureManifest.ts` for the authoritative item list)

## Purpose
Provide the economic spine of a site plan: cost estimates by feature and phase,
revenue stream tagging by enterprise, cashflow / break-even modeling, and
investor / grant / landowner-partnership summaries. This section turns a
regenerative design into something a steward (or funder) can actually fund,
ramp, and defend. P2 delivers pragmatic placeholders (cost/revenue tags,
enterprise buckets, first-pass cashflow); P3 layers the full model (regional
cost DB, contractor bids, sensitivity analysis, ramp-timeline revenue
templates, lean-MVP warnings, TCO and grant-readiness views).

## File inventory
- `EconomicModelingPage.tsx` — route-level entry
- `components/` — section-private components (cost tables, revenue tags,
  cashflow chart, investor summary panels)
- `hooks/` — section-private hooks (cost queries, cashflow computation)
- `economicModelingStore.ts` — Zustand slice (if section owns client state
  for overrides, sensitivity-slider settings, scenario selection)

## Stores touched
- none yet — stubs only. Future wiring will likely read from the project
  store (for phased design features) and the scoring store (for
  mission-weight inputs).

## API routes consumed
- `GET /api/v1/economic-modeling` — stub list endpoint (returns empty).
- Future: `GET/POST /api/v1/economic-modeling/cost-estimates`,
  `.../revenue-streams`, `.../cashflow`, `.../sensitivity`. TBD when the
  P3 model lands.

## Shared schemas
- `@ogden/shared` — `EconomicModelingPlaceholder` (from
  `packages/shared/src/schemas/section22.schema.ts`). Replace with real
  Zod types (CostEstimate, RevenueStream, CashflowEntry, EnterpriseTemplate,
  SensitivityInput) when the model fills in.
- `@ogden/shared/scoring` — not currently read; may eventually consume
  mission-weight scores for donor/grant income modeling.

## Cross-section dependencies
- Section 0f — Regional Cost Database (source of benchmarks and low/mid/high
  ranges used by P2/P3 cost estimates).
- Section 9 — Structures (per-structure cost placeholders feed this model).
- Section 11 — Livestock (enterprise revenue: livestock).
- Section 12 — Crops & Agroforestry (enterprise revenue: orchard).
- Section 15 — Timeline & Phasing (phase allocation drives cost/cashflow
  sequencing).
- Section 16 — Simulation & Scenario Modeling (build-cost / revenue ranges
  overlap; this section owns the economic authority, Section 16 owns the
  scenario sweeps).
- Section 18 — AI (source of the "Overbuilt for revenue" warning and Lean
  MVP path inference).

## Known gotchas
- **Slug collision avoidance.** Two legacy folders already exist and MUST
  NOT be touched by this scaffolding pass:
  - `apps/web/src/features/economics/` — contains `EconomicsPanel.tsx`
    and its CSS module. Likely the origin of current cost/revenue UI.
  - `apps/web/src/features/financial/` — contains `engine/` and `hooks/`.
    Likely the origin of current cashflow / break-even computations.
  The canonical Section 22 slug chosen for this manifest + route prefix is
  `economic-modeling`. A future implementation pass will decide whether to
  migrate the legacy folders into this one or keep them as sub-packages
  imported from here. Until then, the four `status: 'partial'` items in
  the manifest (cost-estimate-by-feature-phase,
  revenue-stream-tagging-enterprise-mapping, cashflow-sequence-chart-break-even,
  investor-summary-landowner-partnership) are presumed covered — at least
  in part — by the legacy folders.
- Phase gating: P3 items are 404'd unless `ATLAS_PHASE_MAX >= P3`. The
  stub route is tagged P2 so it responds at P2+.
- Currency / unit handling is not yet decided. Any numeric field must eventually
  carry an explicit `currency` + `per` (e.g. USD, per-acre vs per-site) to
  avoid the classic cost-model unit-confusion bug.

## Test surface
- No tests yet. Highest-value first tests when the model lands:
  1. Cost roll-up by phase stays consistent when phases are renamed /
     reordered in Section 15.
  2. Cashflow sequence produces a deterministic break-even month given
     fixed cost + ramp inputs.
  3. "Overbuilt for revenue" warning fires only when cumulative cost at
     the end of the ramp window exceeds cumulative revenue by the
     configured threshold.

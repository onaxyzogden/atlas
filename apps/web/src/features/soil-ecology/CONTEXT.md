# CONTEXT — Section 7: Soil, Ecology & Regeneration Diagnostics

> Generated by `scaffold-section.ts`, populated by the Section 7 scaffold
> agent. This file satisfies the MILOS Context-First Protocol for
> `apps/web/src/features/soil-ecology/`.
>
> NOTE: The canonical user-facing surface is **not** this folder. Soil +
> ecology output lands on `features/dashboard/pages/EcologicalDashboard`
> (mounted by `DashboardRouter`) and, for map-based property
> visualization, `features/map/SoilOverlay` (SoilGrids raster + picker).
> This scaffolded folder exists to keep the featureManifest + route
> slotting coherent; the real UI lives where the data does.

## Phase tags
P1, P2 (see `packages/shared/src/featureManifest.ts` for the authoritative
item list). As of 2026-04-24:
- `soil-type-drainage-ssurgo` — **done**. SSURGO drainage class + texture
  + predominant horizon properties flow through `SsurgoAdapter` →
  `site_assessment` → `EcologicalDashboard`. SoilGrids raster overlay
  provides the map-based property visualization for non-US sites.
- `ph-organic-compaction-notes` — **done**. pH / OM / compaction /
  biological-activity entered in the intake wizard
  (`features/project/wizard/StepNotes`) persist to
  `projects.metadata.soilNotes` jsonb and surface on
  `EcologicalDashboard` under a FIELD OBSERVATIONS subsection next to
  the SSURGO-derived values.
- `soil-restoration-opportunity-map` — **done**.
  `RestorationPriorityOverlay` paints `SoilRegenerationProcessor` zone
  centroids on the main Mapbox map as classed circles keyed on
  `properties.priorityClass` (critical/high/moderate/low). Mirrors the
  canonical ViewshedOverlay/MicroclimateOverlay pattern (fetch-on-visible
  + `style.load` re-sync + spine-btn compact toggle). Wired into
  `MapView` + `LeftToolSpine` via `restorationSlot`; state lives at
  `useMapStore.restorationPriorityVisible`.
- `mulching-compost-covercrop-zones` — **done**.
  `MulchCompostCovercropOverlay` filters the same `soil_regeneration`
  FeatureCollection client-side to features whose `primaryIntervention`
  is one of `mulching_priority` / `compost_application` /
  `cover_crop_candidate`, classed by that discriminator (straw / humus /
  young-legume palette). Store: `useMapStore.mulchCovercropVisible`;
  slot: `mulchCovercropSlot` on `LeftToolSpine`.
- `silvopasture-foodforest-regen-zones` — **done**.
  `AgroforestryOverlay` filters `soil_regeneration` to features whose
  `primaryIntervention` is `silvopasture_candidate` or
  `food_forest_candidate`. Caveat: the manifest label names
  "forest regeneration" as a third class, but the processor does not
  yet emit a distinct forest-regeneration intervention type — it is
  currently folded into the food-forest class. Surface this in any UI
  copy; do not fake a separate legend entry. Store:
  `useMapStore.agroforestryVisible`; slot: `agroforestrySlot`.
- All other ecology / regeneration-planning items are P2-planned.

## Purpose
Surface soil character, drainage, and regenerative potential as a
first-class diagnostic layer for a property, and stage the ecology /
regeneration planning tooling (restoration priority, mulching, cover
crop, silvopasture, food-forest, pollinator / biodiversity, invasive
succession, carbon sequestration, intervention log with before/after
comparison). This is the "stewardship-over-force" layer — it tells a
project team what the land is, what it has lost, and what it wants back.
Output feeds Section 4 (scored labels) and Section 11 / 12 (livestock
and cropping placement).

## File inventory

### Scaffolded surface (this folder)
- `SoilEcologyPage.tsx` — route-level entry (stub; real surface is
  `features/dashboard/pages/EcologicalDashboard`).
- `index.ts` — default-export re-export (stub).
- `components/` — (not yet created; section-private components land here
  only if they cannot be hoisted into the EcologicalDashboard page).
- `hooks/` — (not yet created).
- `useSoilEcologyStore.ts` — (not yet created; `useProjectStore` +
  `useSiteDataStore` cover state today).

### Active UI surface (real home of §7 output)
- `apps/web/src/features/dashboard/pages/EcologicalDashboard.tsx` —
  SOIL HEALTH card: core properties (pH, OM, drainage, texture,
  bedrock, tree canopy), PHYSICAL PROPERTIES (bulk density, rooting
  depth, AWC, ksat), PARTICLE SIZE (clay / silt / sand),
  CHEMICAL PROPERTIES (CEC, EC, CaCO₃, SAR), DERIVED INDICES
  (fertility index, salinization risk), FIELD OBSERVATIONS (user
  soilNotes from wizard), plus soil assessment flags. Also renders
  VEGETATION COMMUNITIES, WETLAND & RIPARIAN, ECOLOGICAL
  INTERVENTIONS (from SoilRegenerationProcessor), CARBON
  SEQUESTRATION ESTIMATE, and ECOLOGICAL OPPORTUNITIES.
- `apps/web/src/features/map/SoilOverlay.tsx` — SoilGrids v2.0 raster
  overlay (MapLibre canvas source) with property picker, colour ramp
  legend, and pixel-sampled hover tooltip. Toggled via
  `useMapStore.visibleLayers.has('soil_properties')`.
- `apps/web/src/features/project/wizard/StepNotes.tsx` — SOIL
  OBSERVATIONS form (pH, organic matter, compaction, biological
  activity); round-trips through `projects.metadata.soilNotes` via
  `createProject` → api.projects.create.

### Server-side backing (API) — already shipped
- `apps/api/src/services/terrain/SoilRegenerationProcessor.ts` — computes
  restoration priority zones and regenerative potential from SSURGO
  horizon profile, OM trend, drainage class, slope, land cover.
- `apps/api/src/services/pipeline/adapters/SsurgoAdapter.ts` — US SSURGO
  adapter with multi-horizon profile, restrictive layer detection,
  `chfrags` coarse-fragment table, and drainage-class extraction.
- `apps/api/src/services/pipeline/adapters/OmafraCanSisAdapter.ts` —
  Ontario CanSIS adapter (Canadian parity for SSURGO).
- `apps/api/src/services/soilgrids/SoilGridsRasterService.ts` — global
  fallback soil properties (pH, OC, clay, sand, silt, bulk density,
  CEC) from SoilGrids v2.0 COGs, manifest-driven.
- `apps/api/src/routes/soilgrids/index.ts` — `GET /soilgrids/catalog`,
  `/soilgrids/query`, `/soilgrids/raster`.

### Shared engine — already shipped
- `packages/shared/src/scoring/computeScores.ts` — the regenerative
  potential and agricultural suitability scored labels are driven by
  soil inputs from the adapters above. Any change to soil intake must
  preserve the scoring-parity invariants verified by
  `apps/api/scripts/verify-scoring-parity.ts`.

### Project metadata — already shipped
- `apps/api/src/db/migrations/012_project_metadata.sql` — adds
  `projects.metadata jsonb` which is where free-text soil notes
  (pH / OM / compaction observations) live until they earn dedicated
  columns. Wizard hook: `features/project/wizard/StepNotes.tsx`.

## Stores touched
- `useProjectStore` — active project id, boundary, layer summaries
  (soil data is part of the layer bundle today).
- `useAssessmentStore` (in `features/assessment/`) — holds the
  SoilRegenerationProcessor output cached from the Tier-3 pipeline run.
  Read through that store rather than duplicating fetch logic here.

## API routes consumed
- `GET /api/v1/soil-ecology` — scaffolded stub (returns empty list,
  P1-gated).
- `GET /api/v1/soilgrids/catalog` — list of SoilGrids properties +
  depths available on this install.
- `GET /api/v1/soilgrids/query?lat=…&lon=…&property=…&depth=…` —
  point query against SoilGrids COGs.
- `GET /api/v1/soilgrids/raster/{property}/{depth}/{z}/{x}/{y}.png` —
  tiled SoilGrids overlay for the map.
- `GET /api/v1/projects/:id` — hydrates `site_assessments` + SSURGO /
  SoilGrids layer summaries (includes the regeneration-processor output
  when Tier 3 has run).
- `GET /api/v1/pipeline/:jobId` — soil data arrives via Tier-3 pipeline
  completion; watch this for status before trying to render.

## Shared schemas
- `@ogden/shared` — `ProjectSummary`, `LayerSummary` union (SSURGO and
  SoilGrids layer summaries already live here).
- `@ogden/shared/scoring` — `computeScores`, `ScoreLabel` (this section
  reads `regenerative-potential` and `agricultural-suitability` labels
  but does not compute them).
- `packages/shared/src/schemas/section7.schema.ts` — placeholder Zod
  types. Expand once soil / ecology inputs are lifted out of the legacy
  adapter shapes into canonical `@ogden/shared` types.

## Cross-section dependencies
- §3 Site Data Layers — SSURGO / SoilGrids / NLCD land cover feed both
  soil typing and ecology succession/invasive inference.
- §4 Site Assessment — consumes this section's restoration-priority and
  regenerative-potential output for the scored labels.
- §5 Hydrology — drainage class × topographic wetness index overlaps
  with pond / swale siting; mulching and cover-crop zones look at flow
  accumulation.
- §11 Livestock — silvopasture and grazing candidate zones intersect.
- §12 Crops — cover-crop and food-forest candidate zones intersect.
- §18 AI — carbon-sequestration potential and "what this land wants"
  narratives route through the AI pipeline; do not hand-roll soil
  narratives here.

## Known gotchas
- Coverage asymmetry: SSURGO is US-only; OmafraCanSisAdapter covers
  Ontario; everywhere else falls back to SoilGrids v2.0. Treat the
  SoilGrids path as canonical for global parity and SSURGO as the
  "high-resolution US layer" — do not hard-code either as THE soil
  source.
- `chfrags` (coarse fragment %) is available on the SSURGO path but
  absent from SoilGrids. Any UI surfacing "rockiness" must degrade
  gracefully when `chfrags` is null.
- Drainage-class strings are SSURGO vocabulary ("well drained",
  "somewhat poorly drained", etc). Do not translate — downstream
  scoring pins on the SSURGO string enum.
- Free-text soil fields (pH / OM / compaction / biological activity)
  live under `projects.metadata.soilNotes` jsonb (validated by
  `SoilNotes` in `packages/shared/src/schemas/project.schema.ts`,
  stored via migration 012). Read/write through the existing project
  routes; do not add a side-channel table. The dashboard surfaces
  them verbatim — no parsing — because free-text is intentional
  pre-site-visit.
- Ecology-side items (succession stage, invasive pressure, pollinator
  / biodiversity corridor, carbon sequestration) are all **planned** —
  no server surface exists yet. Do not stub client components that
  pretend to hit endpoints that will 404.
- Regeneration-stage tagging + intervention log + before/after compare
  persist in the `regeneration_events` table (migration 015, shipped
  2026-04-24). Read/write through the typed `RegenerationEvent` +
  `RegenerationEventInput` schemas in `@ogden/shared`. The table
  bundles all three concerns: `event_type='intervention'` +
  `intervention_type` = intervention log; `phase + progress` = stage
  tagging; `parent_event_id` self-FK = before/after pair. Vocabulary
  mirrors `InterventionType` and `SequencePhase` from
  `apps/api/src/services/terrain/algorithms/soilRegeneration.ts` —
  if those TS enums change, update the CHECK constraints in the
  migration AND the Zod enums in `regenerationEvent.schema.ts` in the
  same PR. API routes + UI are NOT yet wired — the substrate is there,
  but `regen-stage-intervention-log` stays `planned` on the manifest
  until routes + timeline UI ship.
- `SoilRegenerationProcessor` output is cached on the Tier-3 pipeline
  run — it does not recompute on UI navigation. Trigger a new run if a
  boundary changes; never synthesize zones client-side.

## Test surface
- `apps/api/scripts/verify-scoring-parity.ts` — must stay green; pins
  the soil-dependent scored labels (regenerative potential,
  agricultural suitability) to the shared-engine output.
- `apps/api/src/services/terrain/__tests__/` — per-processor unit tests
  for SoilRegenerationProcessor.
- `apps/api/src/services/pipeline/adapters/__tests__/` — SsurgoAdapter
  + OmafraCanSisAdapter round-trip tests.
- `apps/api/src/services/soilgrids/__tests__/` — SoilGridsRasterService
  tests (catalog, query, raster tile).
- Highest-value tests to add for this folder:
  1. Smoke test that `GET /api/v1/soil-ecology` returns 200 under
     `ATLAS_PHASE_MAX=P1` (trust `featureGatePlugin`).
  2. Component test that renders a SSURGO-backed soil profile and a
     SoilGrids-backed profile without crashing when one is null.

# CONTEXT — Section 29: Moontrance-Specific Features

> Generated by `scaffold-section.ts`. Fill each section before adding code.
> This file satisfies the MILOS Context-First Protocol for `apps/web/src/features/moontrance-identity/`.

## Phase tags
MT (see `packages/shared/src/featureManifest.ts` for the authoritative item list)

## Purpose
Section 29 surfaces OGDEN Moontrance's identity-defining features — prayer
pavilion siting, Islamic-adab privacy logic, hospitality sequencing, "signs
in creation" interpretive framing, staged vision from raw land to a
heaven-like experience, dawn/sunset viewpoints, fire circles, and the
water/land/worship integration layer. These are "identity features, not
add-ons": the entire section is Moontrance-gated and never ships with the
generic Atlas product. Each item carries a P1/P2/P3 marker preserved in
the label text, but the phase field is `'MT'` for all 16 entries — MT gating
supersedes the P-tag sequencing.

## File inventory
- `MoontranceIdentityPage.tsx` — route-level entry
- `components/` — section-private components
- `hooks/` — section-private hooks
- `MoontranceIdentityStore.ts` — Zustand slice (if section owns client state)

## Stores touched
- (List every `use*Store` consumed or mutated. If none, write "none".)

## API routes consumed
- `GET /api/v1/moontrance-identity` — …
- (List every endpoint. If none, write "none — client-only".)

## Shared schemas
- `@ogden/shared` — (list imported types)
- `@ogden/shared/scoring` — (only if the section reads scores)

## Cross-section dependencies
- (Sections this one reads from. Keep to a minimum.)

## Known gotchas
- **Slug disambiguation.** Two adjacent feature folders already exist and
  are NOT this section:
  - `apps/web/src/features/moontrance/` — legacy Moontrance scratch space
    predating the sectioned manifest.
  - `apps/web/src/features/moontrance-vision/` — Section 14 "Moontrance
    Vision Layer & Concept Overlay" (before/after, stage-by-stage reveal,
    guided flyover). Distinct from this section.
  This §29 is the *identity-focused* scope (prayer pavilion, adab privacy,
  hospitality sequence, staged vision-to-heaven framing) and lives under
  `moontrance-identity/` deliberately.
- **MT gating is mandatory.** Every item's `phase` is `'MT'`, and the
  section `phases: ['MT']`. The entire section is hidden unless
  `ATLAS_MOONTRANCE=1` is set in the environment. `phaseAtMost()` in the
  manifest rejects MT unless the caller explicitly opts in — do not try
  to ship any of these features through the generic `ATLAS_PHASE_MAX`
  gate.
- **P1/P2/P3 markers in labels are informational only.** They reflect the
  spec's intended sequencing *within* the Moontrance timeline; they do not
  change the manifest `phase` field (always `'MT'`) or the gating behavior.
- **Three items marked `status: 'partial'`** reflect identity-leaning work
  that may already exist in the legacy Moontrance Vision Layer (§14):
  `prayer-pavilion-quiet-reflection-siting`, `dawn-sunset-viewpoints`,
  `staged-vision-raw-to-heaven`. Verify with the §14 feature inventory
  before building from scratch.

## Test surface
- (Which `*.test.ts` files cover this section. If none yet, list the
  highest-value tests to add first.)

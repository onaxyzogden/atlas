/**
 * ACT-stage feature exports.
 *
 * Stage 3 of the regenerative-design 3-stage cycle (Observe → Plan → Act).
 * The Act Hub is the landing surface; the spec's five modules ship as
 * `dashboardOnly: true` NavItems registered in
 * `features/navigation/taxonomy.ts` with `stage3: 'act'`.
 */

export { default as ActHub } from './ActHub.js';

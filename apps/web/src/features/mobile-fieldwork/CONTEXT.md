# CONTEXT — Section 24: Mobile, Fieldwork & Site Visit Tools

> Generated by `scaffold-section.ts`. Fill each section before adding code.
> This file satisfies the MILOS Context-First Protocol for `apps/web/src/features/mobile-fieldwork/`.

## Phase tags
P2, P4 (see `packages/shared/src/featureManifest.ts` for the authoritative item list)

## Purpose
Field-side companion to the planning desk: bring the atlas onto the land.
Mobile map with live GPS, geotagged notes and photos, voice memos, checklist
and offline capture (P2), plus the full site-visit toolkit — issue logging,
walk-route recording, on-site measurement, visit report generation, punch
lists, and as-built update mode (P4). Eventually, you'll be on the land,
not staring at it.

## File inventory
- `MobileFieldworkPage.tsx` — route-level entry
- `components/` — section-private components
- `hooks/` — section-private hooks
- `mobileFieldworkStore.ts` — Zustand slice (if section owns client state)

## Stores touched
- (List every `use*Store` consumed or mutated. If none, write "none".)

## API routes consumed
- `GET /api/v1/mobile-fieldwork` — …
- (List every endpoint. If none, write "none — client-only".)

## Shared schemas
- `@ogden/shared` — (list imported types)
- `@ogden/shared/scoring` — (only if the section reads scores)

## Cross-section dependencies
- (Sections this one reads from. Keep to a minimum.)

## Known gotchas
- **Slug choice: `mobile-fieldwork`.** Two legacy folders already carry
  overlapping concerns and must NOT be touched from this section until a
  proper migration plan is approved:
    - `apps/web/src/features/mobile/` — existing `GPSTracker.tsx`,
      `FieldNotes.tsx`. Likely covers part of `mobile-friendly-map-gps`
      and `field-note-geotagged-photo` (hence their `status: 'partial'`).
    - `apps/web/src/features/fieldwork/` — existing `FieldworkPanel.tsx`,
      `SiteChecklist.tsx`, `WalkRouteRecorder.tsx`, `FieldNoteExport.tsx`.
      Overlaps with checklist/walk-route features but uses its own local
      state and component shape.
  New work in `mobile-fieldwork/` is the canonical home; plan a follow-up
  to fold the legacy folders in (after Section 24 is active).
- Offline sync ties into Section 0g (system-wide sync layer) — do not
  invent a local sync protocol here; consume the shared primitive when it
  lands.
- Geotagged photo and voice-memo capture require browser permissions
  (geolocation, camera, microphone). Gate behind explicit opt-in UI.

## Test surface
- (Which `*.test.ts` files cover this section. If none yet, list the
  highest-value tests to add first.)

# CONTEXT — Section 23: Reporting, Export & Presentation

> Generated by `scaffold-section.ts`. Fill each section before adding code.
> This file satisfies the MILOS Context-First Protocol for `apps/web/src/features/reporting-export/`.

## Phase tags
P2, P3, P4 (see `packages/shared/src/featureManifest.ts` for the authoritative item list)

## Purpose
Turns Atlas projects into deliverables usable outside the app: PDF site
assessments, design briefs, phasing/cost/investor/landowner exports,
educational booklets, GIS (KML/GeoJSON) layer exports, image captures,
branded presentation and print layouts, white-label export, and eventually
flyover videos, QR-share links, embeddable views, and public/client handoff
modes. This is the "publishing" surface of the Atlas.

## File inventory
- `ReportingExportPage.tsx` — route-level entry
- `components/` — section-private components (to be added)
- `hooks/` — section-private hooks (to be added)
- `reportingExportStore.ts` — Zustand slice (if section owns client state; not yet created)

## Stores touched
- none yet — stub page has no store wiring.

## API routes consumed
- `GET /api/v1/reporting-export` — stub index handler (returns empty list)
- Legacy `/api/v1/exports/*` endpoints (see `apps/api/src/routes/exports/`)
  cover portions of PDF/GeoJSON export today. To be reconciled with the
  new section routes.

## Shared schemas
- `@ogden/shared` — `Section23Placeholder` (Zod placeholder; replace as the
  section takes shape)

## Cross-section dependencies
- Reads from: Section 4 (Site Assessment), Section 15 (Timeline/Phasing),
  Section 22 (Economic Modeling), Section 14 (Moontrance Vision) for
  investor/landowner/educational exports.

## Known gotchas
- **Legacy directories overlap this section.** Three legacy code locations
  predate the Section 23 scaffold and still hold working logic:
  - `apps/web/src/features/reporting/` — existing reporting UI
  - `apps/web/src/features/export/` — existing export UI
  - `apps/api/src/routes/exports/` — existing export API routes
  Do NOT delete or restructure these during stub scaffolding. They must be
  migrated deliberately as each feature item graduates from `planned` or
  `partial` to `done`. The manifest flags the four items likely already
  partially covered by this legacy code: `pdf-site-assessment-export`,
  `design-brief-feature-schedule-export`, `image-export-screenshot`,
  `gis-kml-geojson-export`.
- The slug `reporting-export` was chosen to disambiguate from both legacy
  folders. Route prefix is `/api/v1/reporting-export` — distinct from the
  legacy `/api/v1/exports`.
- White-label mode (P3) and read-only client handoff (P4) have auth /
  tenant-theming implications that should coordinate with Section 26
  (Admin & Governance).

## Test surface
- No tests yet. Highest-value first tests:
  1. `reporting-export.smoke.test.ts` — GET `/api/v1/reporting-export`
     returns the stub `{ data: [], meta: { total: 0 }, error: null }` shape.
  2. PDF export golden test once the first real generator lands.
  3. KML/GeoJSON roundtrip test for GIS layer export.

/**
 * Shared shim — re-exports the canonical Plan-stage 3D extrusion fallback
 * (`apps/web/src/v3/plan/canvas/layers/DesignElementExtrusionLayer.tsx`)
 * from the unified Built-Environment shared location. See the GLB-layer
 * shim in this directory for the Phase 4.1 / 4.1b split rationale.
 */
export { default } from '../../plan/canvas/layers/DesignElementExtrusionLayer.js';

import StagePagePlaceholder from "./StagePagePlaceholder.js";

export default function ReportPage() {
  return (
    <StagePagePlaceholder
      stage="report"
      title="Project Report"
      subtitle="Generate and share the project summary — verdict, scores, blockers, and next actions."
      buildPhase="Phase 8 — Build + Report MVPs"
    />
  );
}

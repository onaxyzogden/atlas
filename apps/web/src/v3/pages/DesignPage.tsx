import StagePagePlaceholder from "./StagePagePlaceholder.js";

export default function DesignPage() {
  return (
    <StagePagePlaceholder
      stage="design"
      title="Design Studio"
      subtitle="Plan the layout — paddocks, water, paths, structures, and amenities — on the land canvas."
      buildPhase="Phase 9 — Design Studio"
    />
  );
}

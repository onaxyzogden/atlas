/**
 * Homestead-dedicated interventions.
 *
 * The homestead journey is already saturated by the universal
 * `SHARED_INTERVENTIONS` slice (kitchen garden, food forest, poultry,
 * small ruminants, woodlot, solar, water storage, …), so there is no
 * dedicated homestead-only content to add. This module exists for
 * registry symmetry and as the place future homestead-specific entries
 * would land.
 */

import type { Intervention } from '../goalCompassTypes.js';

export const HOMESTEAD_INTERVENTIONS: Intervention[] = [];
